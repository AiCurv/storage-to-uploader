{
  "name": "streamtobuffer-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'no build step'",
    "deploy": "vercel deploy --prod"
  }
}
